Script to transform a folder of .xlsx files to postgres tables

# Usage
`node . folder`

# Enviroment Variables
  - PGUSER :: postgres user
  - PGHOST :: postgres host
  - PGDATABASE :: postgres database
  - PGPASSWORD :: postgres password
